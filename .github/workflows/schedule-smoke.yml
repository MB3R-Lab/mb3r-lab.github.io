name: Schedule smoke test

on:
  schedule:
    - cron: "41 * * * *"
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Print event
        run: |
          echo "event=${GITHUB_EVENT_NAME}"
          date -u +"utc=%Y-%m-%dT%H:%M:%SZ"
